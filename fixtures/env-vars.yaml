# Fixture testing environment variable interpolation
fileName: env.config.json

json:
  database:
    host: ${DB_HOST:-localhost}
    port: ${DB_PORT:-5432}
  api:
    url: ${API_URL}
    timeout: 30000

repos:
  - git: git@github.com:org/repo-env.git
    json:
      serviceName: ${SERVICE_NAME:-default-service}
